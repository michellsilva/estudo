# clock alignment config: one line per source.
#   <source> offset=<seconds> skew=<seconds_per_second> anchor=<iso8601|first>
# reference_ts = raw_ts + offset + skew * (raw_ts - anchor)
#
# deploy is the reference clock: no correction.
# logs host runs 45 s behind the reference, so add 45.
# metric exporter runs 90 s ahead (subtract 90) and drifts fast at 0.02 s/s,
#   anchored at its first sample.
deploy offset=0 skew=0 anchor=first
log offset=45 skew=0 anchor=first
metric offset=-90 skew=0.02 anchor=first
